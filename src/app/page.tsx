import { HeroBanner } from "@/components/blocks/hero-banner";

const Home = () => {
    return (
        <main className="flex flex-col grow">
            <HeroBanner />
        </main>
    );
};

export default Home;
