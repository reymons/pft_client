import { HeroBanner } from "./hero-banner";

const Home = () => {
    return (
        <main className="flex flex-col grow">
            <HeroBanner />
        </main>
    );
};

export default Home;
